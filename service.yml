# Generated by Cloud66 Starter
---
services:

  web:
    git: 
    git_branch: 
    command: bundle exec rails server -e _env:RAILS_ENV
    build_root: .
    ports:
      - container: 3000
        http: 80
        https: 443
    env_vars:
      RAILS_ENV: production
      RACK_ENV: production
